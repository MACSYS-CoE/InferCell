# RHS container benchmark at 32 states (spec task 1.6)

Generated by `dev/scripts/bench_rhs_containers.jl` at commit b14839a, Slurm job 16184994, on dave1, 2026-09-04T08:09:27.842, Julia 1.10.5.
Composition: EnergyPools (13 states) + CarbonBlock (19 states), two contributions. 
Solves: tspan (0, 6300) s, abstol 1e-10, reltol 1e-8, saveat 60 s, best of 5.

| RHS | bytes / call | ns / call | Tsit5 solve (s) | Rodas5P solve (s) |
|---|---|---|---|---|
| legacy out-of-place (no contributions; Vector->SVector conversion added) | 3152 | 2087 | 0.0039 | 0.0184 |
| phase-1 static out-of-place | 0 | 26 | 0.0004 | 0.0076 |
| hand-written in-place Vector | 0 | 11 | 0.0002 | 0.0042 |


Single-gene `TranscriptionTranslation` (2 states, no contributions), the model the byte-identity fixture pins:

| RHS | bytes / call | ns / call |
|---|---|---|
| TranscriptionTranslation, legacy | 560 | 549 |
| TranscriptionTranslation, phase-1 static | 224 | 866 |

Its own `dynamics` branches on `gene_names` at run time and is not inferable, so the framework's 
zero-allocation path cannot make the composed function allocation-free; the rows show what the 
framework change alone saved for it.

The legacy row omits the two cross-module terms because that RHS has no channel for them, 
and includes a Vector-to-SVector conversion of its result: its local state slice is a plain 
Vector, so a broadcasting dynamics returns one and OrdinaryDiffEq refuses the solve without it. 
The real models avoided that only by returning SA[...] explicitly.
