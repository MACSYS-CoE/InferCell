# The assembled Core A′ over one full cycle (spec tasks 13.5, 13.7)

Generated by `dev/scripts/corea_full_cycle.jl` at commit 28acc08, Slurm job 17298242, on gina10, 2026-09-25T08:03:27.149, Julia 1.10.5.

Regenerate with `sbatch dev/scripts/corea_full_cycle.slurm`.

## 13.5 — the build and the solver

`build_corea()` built in completeness mode: no unowned state, no dead end, 89 declared edges and 0 of them unexecuted.
Solver recorded on the driver: `Rodas5P`, abstol 1.0e-10, reltol 1.0e-8. Drain every 1.0 s, rounding `fractional_carry`.

The cycle completed: t = 6300.0 s after 6300 handshakes, with no solver failure (the hook throws on one).

## 13.7 — wall-clock per trajectory

Build 28.1 s; first handshake (compiles) 81.8 s; the remaining 6299 handshakes **32.2 s** (5.11 ms each).
A warm trajectory is therefore **32.2 s** of stepping, plus 28.1 s to build. A process pays the 82 s compile once, not per trajectory.
Against phase 3's toy extrapolation of 0.019 s per cycle that is 1695× slower; against phase 11's partial seven-module 17.9 s (job 17131232, without transcription's counters) it is 1.80×.
One core runs 112 warm trajectories an hour; 50 calibration replications of a posterior needing E sequential evaluations cost 50·E·32.2 core-seconds, which is K1's input once task 16.2 fixes E (spec §8 K1).

## What phase 11's partial run reported, remeasured with every counter and credit

Check 7's census: 253 of 6300 drains clipped (fraction 0.0402). Not the K5 score, which is phase 14's.

| counter | drains clipped | first clip (s) |
|---|---|---|
| ATP_trsc | 0 | never |
| ATP_mRNA | 0 | never |
| GTP_mRNA | 0 | never |
| CTP_mRNA | 0 | never |
| UTP_mRNA | 0 | never |
| ATP_mRNAdeg | 0 | never |
| AMP_mRNAdeg | 0 | never |
| GMP_mRNAdeg | 0 | never |
| CMP_mRNAdeg | 0 | never |
| UMP_mRNAdeg | 0 | never |
| GTP_translat | 253 | 953 |
| tRNA_translat | 20 | 1603 |
| ATP_transloc | 0 | never |
| ptsI_translat | 0 | never |
| ptsH_translat | 0 | never |
| Crr_translat | 0 | never |
| ptsG_transloc | 0 | never |

Minimum over the cycle, and every 60 s, first and last five samples (mM):

- `M_atp_c`: min 0.7781 (16695 particles at the final volume); 1.8949, 2.0436, 1.6544, 1.7781, 1.6221 … 1.0750, 1.0585, 0.8948, 1.0066, 0.9033
- `M_gtp_c`: min 0 (0 particles at the final volume); 1.2343, 0.5739, 0.2549, 0.2335, 0.3708 … 0.0936, 0.0000, 0.1387, 0.0849, 0.0454
- `M_trna_c`: min 0.003665 (79 particles at the final volume); 0.0424, 0.0587, 0.0873, 0.1237, 0.0690 … 0.1564, 0.1980, 0.0510, 0.1472, 0.1108
- `M_trna_chg_c`: min 0 (0 particles at the final volume); 0.2075, 0.1912, 0.1625, 0.1260, 0.1805 … 0.0801, 0.0383, 0.1849, 0.0883, 0.1244

R11: the charged pool held under one particle at 0 of 105 rebuild instants.

Protein fold change (phase 11's definition): median **1.627**, min 1.215, max 2.193. Phase 11's partial run gave 1.497; §3's band [1.7, 2.3] is phase 14's F5 to judge.
Fractional growth 1.0627.

## What is ours in this run

```
93 declaration(s) that are this reduction's, not the published model's:
  clamp:
    - clamp on :M_ctp_c is this reduction's simplification, not the published model's
    - clamp on :M_utp_c is this reduction's simplification, not the published model's
  calibrated_constant:
    - surface area accumulates at 28.0 nm² per membrane protein, which is the published model's *calibrated* constant — chosen to reproduce 54% membrane coverage for its ~9,600 membrane proteins — and not a measurement of a protein. `getProtSA`'s own docstring says 35 nm² while its code uses 28.0; the code is what runs, so the code is what is ported, and the contradiction is recorded rather than resolved
  exogenous_growth:
    - 479550.8 nm² of the initial 502831.0 nm² surface area is held frozen: the lipid leaflet and every membrane-protein locus outside this reduction are exogenous, so only the flagged proteins grow the cell. **Ours, not the published model's**, which grows both terms — and it is why growth reaches ~1.07× rather than approaching the 2× cap the published model stops at
  asserted_prior:
    - prior on :kf_glcpts0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kr_glcpts0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kf_glcpts1 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kr_glcpts1 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kf_glcpts2 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kr_glcpts2 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kf_glcpts3 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kr_glcpts3 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kf_glcpts4 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :kr_glcpts4 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :p_lact2r is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :lac_volume_ratio is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :glc_e_mM is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_lac__L_e0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsi_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsi_P_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsh_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsh_P_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_crr_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_crr_P_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsg_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :M_ptsg_P_c0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :k_chg is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :trna_pool_mM is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :trna_charged_fraction is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0445 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0220 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0131 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0727 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0607 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0606 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0729 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0213 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0221 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0475 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0233 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0234 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0694 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0779 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0651 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0344 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :S_JCVISYN3A_0203 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tx_rnapol_kcat is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tx_rnapol_k0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tx_rnapol_kd is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tx_rnap_conc is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :krnadeg is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tl_ribo_kcat is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tl_ribo_k0 is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tl_ribo_kd is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tl_ribo_conc is asserted by this project — its source carries a point value with no quantified uncertainty
    - prior on :tl_transloc_k is asserted by this project — its source carries a point value with no quantified uncertainty
  discarded_prior:
    - prior on :enz_R_PGI is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PFK is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_FBA is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_TPI is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_GAPD is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PGK is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PGM is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_ENO is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PYK is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_LDH_L is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :r_cell_nm is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PGK3 is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PYK3 is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_ADK1 is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_GK1 is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
    - prior on :enz_R_PPA is asserted by this project and discarded: the driver overwrites the slot at every handshake, so a posterior for it is its prior
  lumping:
    - tRNA charging lumped: one reaction M_trna_c + ATP -> M_trna_chg_c + AMP + PPi replaces the published 20 per-amino-acid chains of 5 reactions each (100 reactions), with one effective tRNA pool whose size and charged fraction are asserted by this project. The published AMP + PPi product stoichiometry is kept in preference to a 2 ATP -> 2 ADP + 2 Pi form, which would close the same moieties by fiat and route no traffic through the adenylate kinase
  formalism:
    - tRNA charging integrated deterministically in the ODE block: the placement is the scoping note's, but the formalism is this project's; the published model fires charging as ~3.49 million stochastic events per cycle (spec §4 D13)
  model_note:
    - the terminal oxidase NOX is dropped from the reaction list: LDH_L already regenerates NAD+ from NADH so NOX is redundant for redox balance, it carries no gene-protein-reaction rule so cutting it crosses no boundary, and its catalytic constant was prior-dominated so it was never an inference target; this is a departure from the published reaction list rather than from the published model's behaviour
    - the 32 Michaelis constants of R_PGI, R_PFK, R_FBA, R_TPI, R_GAPD, R_PGK, R_PGM, R_ENO, R_PYK and R_LDH_L are imported from the balanced Parameter table rather than the Quantity table the published simulator reads, because only the balanced column carries an inherited prior; eight of the 32 differ, one by 82 times and one by 227 times (km_R_PGI_M_g6p_c runs at 0.28 and is imported at 22.9419; km_R_PGI_M_f6p_c runs at 0.15 and is imported at 3.3488; km_R_PFK_M_atp_c runs at 0.117 and is imported at 0.0255; km_R_FBA_M_fdp_c runs at 0.005 and is imported at 0.2447; km_R_FBA_M_dhap_c runs at 0.095 and is imported at 0.0064; km_R_FBA_M_g3p_c runs at 1.0 and is imported at 0.0044; km_R_GAPD_M_nad_c runs at 1.3 and is imported at 5.6969; km_R_PGM_M_2pg_c runs at 1.47 and is imported at 0.0278, all in mM)
    - the consequence of that column choice is not confined to eight inputs: at the registry's initial concentrations R_PGI runs backwards on the imported constants, -0.3325 mM/s against +4.5963 mM/s on the constants the published simulator runs, so the sign of the pathway's entry reaction is ours rather than the published model's; R_FBA is slower by 253 times, 0.00901 against 2.27985 mM/s. Both reverse once the pools move, and the module still integrates non-negatively over a full cycle, but no statement about an initial flux here is the published model's
    - M_atp_c, M_adp_c and M_pi_c are read and contributed to but not integrated here, so any trajectory produced without the nucleotide recycling module composed holds them at whatever the double supplies and is not a closed energy loop
    - the ten enzyme concentrations are free slots enz_R_<reaction> that catalytic edges overwrite from the protein counts at every handshake, so their declared values -- the published copy numbers at the registry's cell -- hold only before the first one, and their priors, asserted by this project, describe draws the driver discards
    - external lactate accumulates at 1/R of the export rate with R declared at 100000.0, a medium-to-cell volume ratio asserted by this project: it reproduces the published model's constant external pool, which clamps M_lac__L_e and so never saturates the exporter, through the dynamic state the registry requires. Chosen against a stated criterion — external lactate must stay below one percent of steady cytosolic lactate over a full cycle, which 1e5 meets at 0.47% and 1e4 misses at 4.7%
    - the amino-acid pool :M_aa_pool_c is a registry chemostat held at an asserted 0.1 mM, and no Core A′ reaction reads it: the lumped step charges tRNA from ATP alone, so the amino acids the published chains consume are neither drawn nor limiting. Ours, since its sources are in modules Core A′ cuts (spec §3)
    - The base-to-nucleotide mapping is corrected: each base is charged against the nucleotide it is polymerised from (:corrected), rather than against the permutation of MinCell_CMEODE.py:377-383 (:published), where the UTP term takes the cytosine count, the CTP term the guanine count and the GTP term the uracil count. The rate constants move about one percent; the sensitivity to the live GTP pool moves by a median 1.85× across the seventeen, spanning 1.53 to 2.45 — it is the per-gene U-to-G count ratio, not one factor. GTP is one of four reverse channels, not the only one: ATP, CTP and UTP enter the same rate law, and the measured ATP elasticity matches GTP's. Both mappings are computable.
    - CTP and UTP are held constant at 0.6874 and 2.7681 mM. This reduction chemostats them, not the published model: the reactions that make them are in modules Core A′ cuts. The assumption stops being defensible the moment either pool is made live.
    - Promoter strength is the published proteomics proxy, protein copy number over 180, inherited verbatim. It is not a measured parameter — it is back-calculated from the steady-state protein abundance the model is meant to predict.
    - Conditioning on proteomics would be circular. The same protein counts that set these seventeen promoter strengths also set every enzyme concentration in the metabolic modules, so a protein-level observable closes a loop through them. Affected: k_tx_JCVISYN3A_0445, k_tx_JCVISYN3A_0220, k_tx_JCVISYN3A_0131, k_tx_JCVISYN3A_0727, k_tx_JCVISYN3A_0607, k_tx_JCVISYN3A_0606, k_tx_JCVISYN3A_0729, k_tx_JCVISYN3A_0213, k_tx_JCVISYN3A_0221, k_tx_JCVISYN3A_0475, k_tx_JCVISYN3A_0233, k_tx_JCVISYN3A_0234, k_tx_JCVISYN3A_0694, k_tx_JCVISYN3A_0779, k_tx_JCVISYN3A_0651, k_tx_JCVISYN3A_0344, k_tx_JCVISYN3A_0203.
    - ATP is debited twice per transcript — once as polymerisation energy at one per nucleotide (ATP_trsc), once as an incorporated monomer (ATP_mRNA). This is the published model's accounting, not an error, and it is recorded so that it is not "fixed".
    - Genes are carried as a fixed quantity rather than as states, because Core A′ cuts replication and nothing can change them. The published model carries them as species; adding replication must promote them.
    - Transcription is constitutive: one rate constant per gene, no promoter switching. The two-state identifiability result does not transfer, because there is no on/off rate pair to be degenerate.
    - Decay's CMP and UMP are credited into the CTP and UTP chemostats. Core A′ has no CMP or UMP species, and the published model's M_cmp_c and M_ump_c sit in modules this reduction cuts, so the chemostat absorbs them. A monophosphate credited to a triphosphate pool is not a reaction; it is an exemption, and it stops being defensible the moment either pool is made live.
    - Translation's rate law reads the lumped charged-tRNA pool through its per-amino-acid share, [M_trna_chg_c]/20, in each of the twenty-one concentrations the published law reads per amino acid (translation_rate_restart.py). This keeps the law's structure: 0.01 mM at the nominal pool against upstream's 150 copies, 0.0074 mM. Reading the whole pool instead would cut the charged-tRNA elasticity about twenty-fold. The lumping itself is TrnaCharging's declaration, not this one.
    - Translation runs the published restart law (riboKd 1e-3, kcat_mod (0.25n + 0.2)·riboKcat) from the start of the cycle. The published model runs the start law (riboKd 1e-4, +0.25) for its first minute only.
    - A residue excludes the stop codon, the convention k_chg was calibrated on. Upstream charges GTP on len(aasequence), which counts the stop, so this module charges two GTP per protein fewer than the published model.
  capped_rate_law_geometry:
    - above the growth cap, the radius and area a rate law receives through an inbound VolumeEdge are reconstructed from the *capped* volume, so the three quantities still describe one sphere. Below the cap nothing is reconstructed and the delivered geometry is the reported one exactly; the delivered *volume* is the reported one everywhere, since above the cap it is the cap. **Ours**: the published model caps the volume and reports an uncapped radius, which is harmless while the radius is only reported and wrong once it drives a rate law, because `3P/r` is the surface-to-volume ratio of a sphere and the cap is exactly what breaks that identity
  driver_policy (recorded whether or not it departs; not counted above):
    - the deferred counters are debited at every 1.0 s handshake, the published model's granularity; a coarser drain would be a labelled reduction (spec §4 D10)
    - counts are written back under fractional carry, this project's policy (spec §3 check 0) and the only one whose round trip is exact
```
