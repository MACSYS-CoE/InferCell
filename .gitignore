# Ignore all dotfiles/dotdirs except .gitignore (also covers .DS_Store everywhere)
.*
!.gitignore

# Ignore root markdown except the few we track
/*.md
!README.md
!handoff.md
!log.md

# Slurm/job logs and core dumps anywhere
*.log
core.*

# LaTeX build artifacts (talks/)
*.aux
*.fdb_latexmk
*.fls
*.nav
*.out
*.snm
*.synctex.gz
*.toc
*.vrb

# examples/ for now — a clean demo will be re-added later
examples/

# MkDocs build output
/site/

# Non-published content kept out of the repo deliberately:
# private grant drafts, council transcripts, large reference PDFs.
# (gitignore does not support trailing inline comments — keep these on their own lines)
/grant/
/research/council/
/research/references/

# GitHub Actions (the leading `.*` rule otherwise hides the whole tree)
!.github/
!.github/workflows/
!.github/workflows/*.yml
